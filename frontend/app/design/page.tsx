"use client";

import { AgentWorkspace } from "../workspace";

export default function DesignPage() {
  return (
    <AgentWorkspace
      focus="design"
      title="Agent Design"
      intro="Direction visuelle et formats de publication, alignés sur la priorité décidée par l'agent Marketing."
    />
  );
}
